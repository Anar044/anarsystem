import { clean, getOlapFields, iikoJson } from "./_lib/iiko-client.js";

function corsHeaders(){return{"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type"};}
function jsonResponse(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store",...corsHeaders()}});}
function fieldName(fields,candidates){const norm=s=>clean(s).toLowerCase().replace(/[\s._()-]+/g,"");for(const c of candidates){const x=fields.find(f=>norm(f.name)===norm(c)||norm(f.title)===norm(c));if(x)return x.name}for(const c of candidates){const q=norm(c),x=fields.find(f=>norm(f.name).includes(q)||norm(f.title).includes(q));if(x)return x.name}return null;}
async function query(connection,{dateField,revenueField,orderField,profitField,departmentField,from,to,departmentIds}){const filters={};filters["OpenDate.Typed"]={filterType:"DateRange",periodType:"CUSTOM",from,to,includeLow:true,includeHigh:true};if(departmentIds.length&&departmentField)filters[departmentField]={filterType:"IncludeValues",values:[...new Set(departmentIds)]};filters.DeletedWithWriteoff={filterType:"IncludeValues",values:["NOT_DELETED"]};filters.OrderDeleted={filterType:"IncludeValues",values:["NOT_DELETED"]};const aggregateFields=[revenueField,...(orderField?[orderField]:[]),...(profitField?[profitField]:[])];const req={reportType:"SALES",buildSummary:true,groupByRowFields:[dateField],groupByColFields:[],aggregateFields,filters};const r=await iikoJson(connection,"/resto/api/v2/reports/olap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(req)});if(!r.ok)return{success:false,error:`iiko OLAP HTTP ${r.status}: ${(r.payload?.message||r.payload?.error||r.text).slice(0,2000)}`,request:req,report:r.payload,authCacheHit:r.auth?.cacheHit===true};return{success:true,request:req,report:r.payload,authCacheHit:r.auth?.cacheHit===true};}
export function onRequestOptions(){return new Response(null,{status:204,headers:corsHeaders()});}
export async function onRequestPost(context){try{const b=await context.request.json();const connection={ip:clean(b.ip),port:clean(b.port),login:clean(b.login),password:String(b.password??"")};if(!connection.ip||!connection.port||!connection.login||!connection.password)return jsonResponse({success:false,message:"Заполните IP, порт, логин и пароль iiko"},400);const from=clean(b.from).slice(0,10),to=clean(b.to||b.from).slice(0,10);if(!/^\d{4}-\d{2}-\d{2}$/.test(from)||!/^\d{4}-\d{2}-\d{2}$/.test(to))return jsonResponse({success:false,message:"Укажите корректный период"},400);const fieldResult=await getOlapFields(connection,"SALES");const fields=fieldResult.fields;const dateField=fieldName(fields,["OpenDate.Typed","OpenDate"]),revenueField=fieldName(fields,["DishDiscountSumInt","DishSumInt","Sales"]),orderField=fieldName(fields,["UniqOrderId","UniqOrderId.Id"]),profitField=fieldName(fields,["ProductCostBase.Profit","Profit"]),departmentField=fieldName(fields,["Department.Id","DepartmentId","Department.ID"]);if(!dateField||!revenueField)throw new Error("Не найдены необходимые OLAP-поля");const departmentIds=Array.isArray(b.departmentIds)?[...new Set(b.departmentIds.map(clean).filter(Boolean))]:[];if(departmentIds.length&&!departmentField)throw new Error("Не найдено OLAP-поле Department.Id — нельзя безопасно ограничить Dashboard выбранным рестораном");const daily=await query(connection,{dateField,revenueField,orderField,profitField,departmentField,from,to,departmentIds});return jsonResponse({success:true,from,to,departmentIds,fields,revenueField,orderField,profitField,departmentField,reports:{daily},meta:{sharedIikoClient:true,authCacheHit:daily.authCacheHit===true,olapFieldsCacheHit:fieldResult.cacheHit===true,departmentScopeApplied:departmentIds.length>0}});}catch(e){return jsonResponse({success:false,message:e?.message||"Ошибка Dashboard Summary"},502);}}
