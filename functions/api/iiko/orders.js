import { clean, getOlapFields, iikoJson } from "./_lib/iiko-client.js";

function corsHeaders(){return{"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type"};}
function jsonResponse(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store",...corsHeaders()}});}
function fieldName(fields,candidates){const norm=s=>clean(s).toLowerCase().replace(/[\s._()-]+/g,"");for(const c of candidates){const x=fields.find(f=>norm(f.name)===norm(c)||norm(f.title)===norm(c));if(x)return x.name;}for(const c of candidates){const q=norm(c);const x=fields.find(f=>norm(f.name).includes(q)||norm(f.title).includes(q));if(x)return x.name;}return null;}
function buildRequest({rows,measure,from,to,departmentField,departmentIds}){const filters={"OpenDate.Typed":{filterType:"DateRange",periodType:"CUSTOM",from,to,includeLow:true,includeHigh:true},DeletedWithWriteoff:{filterType:"IncludeValues",values:["NOT_DELETED"]},OrderDeleted:{filterType:"IncludeValues",values:["NOT_DELETED"]}};if(departmentField&&departmentIds.length)filters[departmentField]={filterType:"IncludeValues",values:departmentIds};return{reportType:"SALES",buildSummary:true,groupByRowFields:rows,groupByColFields:[],aggregateFields:[measure],filters};}
function number(v){if(typeof v==="number")return v;return Number.parseFloat(String(v??"").replace(",","."))||0;}
async function query(connection,request){const r=await iikoJson(connection,"/resto/api/v2/reports/olap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(request)});if(!r.ok)return{success:false,error:`iiko OLAP HTTP ${r.status}: ${(r.payload?.message||r.payload?.error||r.text).slice(0,2000)}`,authCacheHit:r.auth?.cacheHit===true};return{success:true,report:r.payload,authCacheHit:r.auth?.cacheHit===true};}
function rowsFromReport(report){return report&&Array.isArray(report.data)?report.data:[];}
export async function onRequestOptions(){return new Response(null,{status:204,headers:corsHeaders()});}
export async function onRequestPost(context){try{const b=await context.request.json();const connection={ip:clean(b.ip),port:clean(b.port),login:clean(b.login),password:String(b.password??"")};if(!connection.ip||!connection.port||!connection.login||!connection.password)return jsonResponse({success:false,message:"Заполните IP, порт, логин и пароль iiko"},400);const from=clean(b.from).slice(0,10),to=clean(b.to||b.from).slice(0,10);if(!/^\d{4}-\d{2}-\d{2}$/.test(from)||!/^\d{4}-\d{2}-\d{2}$/.test(to))return jsonResponse({success:false,message:"Укажите корректный период"},400);const departmentIds=[...new Set((Array.isArray(b.departmentIds)?b.departmentIds:[]).map(clean).filter(Boolean))];const fieldResult=await getOlapFields(connection,"SALES");const fields=fieldResult.fields;const dateField=fieldName(fields,["OpenDate.Typed","OpenDate"]);const orderField=fieldName(fields,["OrderNum","OrderNumber","Order.Number","OrderNum.Id","OrderId","Order.Id"]);const tableField=fieldName(fields,["TableName","Table.Name","Table"]);const waiterField=fieldName(fields,["WaiterName","Waiter.Name","EmployeeName","Waiter"]);const statusField=fieldName(fields,["OrderStatus","Order.Status","Status"]);const revenueField=fieldName(fields,["DishDiscountSumInt","DishSumInt","Sales"]);const departmentField=fieldName(fields,["Department.Id","DepartmentId","Department.ID"]);if(!dateField||!revenueField)return jsonResponse({success:false,message:"Не найдены необходимые OLAP-поля заказа",fields},502);if(departmentIds.length&&!departmentField)return jsonResponse({success:false,message:"Не найдено OLAP-поле Department.Id — нельзя безопасно ограничить список заказов выбранным рестораном"},502);if(!orderField){return jsonResponse({success:true,available:false,from,to,orders:[],summary:{orders:0,revenue:0},message:"В iiko не найдено поле номера заказа для списка заказов",fields,meta:{sharedIikoClient:true,olapFieldsCacheHit:fieldResult.cacheHit===true,departmentScopeApplied:false}});}const rows=[orderField,dateField];if(tableField)rows.push(tableField);if(waiterField)rows.push(waiterField);if(statusField)rows.push(statusField);const result=await query(connection,buildRequest({rows,measure:revenueField,from,to,departmentField,departmentIds}));if(!result.success)return jsonResponse({success:false,message:result.error,fields},502);const orders=rowsFromReport(result.report).map((raw,index)=>{const orderNumber=clean(raw[orderField]??raw.OrderNum??raw.OrderNumber)||`#${index+1}`;return{number:orderNumber,date:clean(raw[dateField]??raw.OpenDate),table:tableField?clean(raw[tableField]):"",waiter:waiterField?clean(raw[waiterField]):"",status:statusField?clean(raw[statusField]):"",amount:number(raw[revenueField]??raw.DishSumInt??raw.DishDiscountSumInt)};}).filter(x=>x.number);const revenue=orders.reduce((sum,x)=>sum+x.amount,0);return jsonResponse({success:true,available:true,from,to,orders,summary:{orders:orders.length,revenue},fields:{orderField,dateField,tableField,waiterField,statusField,revenueField,departmentField},meta:{sharedIikoClient:true,authCacheHit:result.authCacheHit,olapFieldsCacheHit:fieldResult.cacheHit===true,departmentScopeApplied:departmentIds.length>0,departmentIds}});}catch(e){return jsonResponse({success:false,message:e?.message||"Ошибка получения заказов"},502);}}
