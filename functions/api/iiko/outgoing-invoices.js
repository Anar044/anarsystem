import { syncReferences } from "./references.js";

function corsHeaders(){return {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type, Authorization","Content-Type":"application/json; charset=utf-8"};}
function jsonResponse(data,status=200){return new Response(JSON.stringify(data),{status,headers:{...corsHeaders(),"Cache-Control":"no-store"}});}
async function sha1(text){const data=new TextEncoder().encode(text);const hash=await crypto.subtle.digest("SHA-1",data);return Array.from(new Uint8Array(hash)).map(b=>b.toString(16).padStart(2,"0")).join("");}
async function auth(ip,port,login,password){const serverUrl=`http://${ip}:${port}`;const pass=await sha1(password);const r=await fetch(`${serverUrl}/resto/api/auth?login=${encodeURIComponent(login)}&pass=${pass}`,{cache:"no-store"});const token=(await r.text()).trim();if(!r.ok||!token)throw new Error(`Ошибка авторизации SH Server: HTTP ${r.status}`);return {serverUrl,token};}
function xmlDecode(value){return String(value??"").replace(/&lt;/g,"<").replace(/&gt;/g,">").replace(/&quot;/g,'"').replace(/&apos;/g,"'").replace(/&amp;/g,"&");}
function tag(block,name){const m=String(block||"").match(new RegExp(`<${name}(?:\\s[^>]*)?>([\\s\\S]*?)</${name}>`,`i`));return m?xmlDecode(m[1].trim()):"";}
function blocks(source,name){const out=[];const re=new RegExp(`<${name}(?:\\s[^>]*)?>[\\s\\S]*?</${name}>`,`gi`);let m;while((m=re.exec(String(source||""))))out.push(m[0]);return out;}
function number(v){const n=Number(String(v??"").replace(/\\s/g,"").replace(",","."));return Number.isFinite(n)?n:null;}
function parseItems(block){return blocks(block,"item").map((b,i)=>({num:tag(b,"num")||String(i+1),productId:tag(b,"productId")||tag(b,"product"),productArticle:tag(b,"productArticle"),storeId:tag(b,"storeId")||tag(b,"store"),storeCode:tag(b,"storeCode"),containerId:tag(b,"containerId"),containerCode:tag(b,"containerCode"),price:number(tag(b,"price")),priceWithoutVat:number(tag(b,"priceWithoutVat")),amount:number(tag(b,"amount")),sum:number(tag(b,"sum")),discountSum:number(tag(b,"discountSum")),vatPercent:number(tag(b,"vatPercent")),vatSum:number(tag(b,"vatSum"))}));}
function parseDocuments(xml){return blocks(String(xml||"),"document").map((b,i)=>{const items=parseItems(b);return {id:tag(b,"id")||null,documentNumber:tag(b,"documentNumber")||null,dateIncoming:tag(b,"dateIncoming")||null,status:tag(b,"status")||null,accountToCode:tag(b,"accountToCode")||null,revenueAccountCode:tag(b,"revenueAccountCode")||null,defaultStoreId:tag(b,"defaultStoreId")||tag(b,"defaultStore")||null,defaultStoreCode:tag(b,"defaultStoreCode")||null,counteragentId:tag(b,"counteragentId")||tag(b,"counteragent")||null,counteragentCode:tag(b,"counteragentCode")||null,comment:tag(b,"comment")||null,linkedOutgoingInvoiceId:tag(b,"linkedOutgoingInvoiceId")||null,sum:items.reduce((s,x)=>s+(x.sum||0),0),itemsCount:items.length,items,rawIndex:i};}).filter(d=>d.documentNumber||d.id||d.itemsCount);}
function key(v){return String(v??"").trim().replace(/^\{+|\}+$/g,"").toLowerCase();}
function dateParts(value){const s=String(value||"").trim();if(/^\d{4}-\d{2}-\d{2}$/.test(s)){const p=s.split("-");return {iso:s,d:p[2],m:p[1],y:p[0]};}if(/^\d{2}\.\d{2}\.\d{4}$/.test(s)){const p=s.split(".");return {iso:`${p[2]}-${p[1]}-${p[0]}`,d:p[0],m:p[1],y:p[2]};}return null;}
function dateFormats(value){const p=dateParts(value);return p?[String(value).trim(),`${p.d}.${p.m}.${p.y}`]:[];}
function dateKey(value){return dateParts(value)?.iso||"";}
function applyNames(docs,refs){return docs.map(d=>{const counteragentName=refs.suppliers.get(key(d.counteragentId))||d.counteragentId||"—";const storeName=refs.warehouses.get(key(d.defaultStoreId))||d.defaultStoreId||"—";const items=(d.items||[]).map(x=>({...x,productName:refs.products.get(key(x.productId))||x.productId||"—",storeName:refs.warehouses.get(key(x.storeId||d.defaultStoreId))||x.storeId||d.defaultStoreId||"—"}));return {...d,counteragentName,storeName,items};});}
async function requestXml(serverUrl,path){const r=await fetch(`${serverUrl}${path}`,{cache:"no-store",headers:{Accept:"application/xml,text/xml,*/*"}});const text=await r.text();return {ok:r.ok,status:r.status,text,contentType:r.headers.get("content-type")||""};}
async function getInvoices(serverUrl,token,from,to,counteragentId){const attempts=[];const formats=[...dateFormats(from)],toFormats=[...dateFormats(to)];for(const f of formats)for(const t of toFormats){const p=new URLSearchParams({key:token,from:f,to:t});if(counteragentId)p.set("supplierId",counteragentId);const r=await requestXml(serverUrl,`/resto/api/documents/export/outgoingInvoice?${p.toString()}`);const docs=r.ok?parseDocuments(r.text):[];attempts.push({from:f,to:t,status:r.status,ok:r.ok,documents:docs.length});if(r.ok&&docs.length)return {docs,attempts};}
const p=new URLSearchParams({key:token});if(counteragentId)p.set("supplierId",counteragentId);const fallback=await requestXml(serverUrl,`/resto/api/documents/export/outgoingInvoice?${p.toString()}`);const docs=fallback.ok?parseDocuments(fallback.text):[];return {docs:docs.filter(d=>{const k=dateKey(d.dateIncoming);return !k||(!dateKey(from)||k>=dateKey(from))&&(!dateKey(to)||k<=dateKey(to));}),attempts:[...attempts,{fallback:true,status:fallback.status,ok:fallback.ok,documents:docs.length}],serverDocuments:docs.length};}
export async function onRequestOptions(){return new Response(null,{status:204,headers:{...corsHeaders()}});}
export async function onRequestPost(context){try{const b=await context.request.json();const ip=String(b.ip||"").trim(),port=String(b.port||"").trim(),login=String(b.login||"").trim(),password=String(b.password||"");if(!ip||!port||!login||!password)return jsonResponse({success:false,message:"Заполните IP, порт, логин и пароль SH Server"},400);if(!dateParts(b.from)||!dateParts(b.to))return jsonResponse({success:false,message:"Укажите период"},400);if(dateKey(b.to)<dateKey(b.from))return jsonResponse({success:false,message:"Дата «По» раньше даты «С»"},400);const {serverUrl,token}=await auth(ip,port,login,password);const result=await getInvoices(serverUrl,token,b.from,b.to,b.counteragentId);const needed=[...new Set(result.docs.map(d=>key(d.counteragentId)).filter(Boolean))];const refs=await syncReferences(context.env,serverUrl,token,needed).catch(e=>({maps:{suppliers:new Map(),warehouses:new Map(),products:new Map()},diagnostics:{error:String(e?.message||e)}}));const maps=refs.maps||refs;const documents=applyNames(result.docs,maps);return jsonResponse({success:true,count:documents.length,from:b.from,to:b.to,documents,referenceSource:"iiko-sync+d1",attempts:result.attempts,serverDocuments:result.serverDocuments||result.docs.length});}catch(e){return jsonResponse({success:false,message:e?.message||"Ошибка получения расходных накладных"},502);}}