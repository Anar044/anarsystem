(function(){
'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cells=r=>r?[...r.children]:[];
const text=(r,i)=>(cells(r)[i]?.textContent||'').trim();
function copyMeasures(target,source){const tc=cells(target),sc=cells(source);if(tc.length<2||sc.length<2)return;const count=Math.min(2,tc.length,sc.length);for(let i=0;i<count;i++){const v=(sc[sc.length-count+i]?.textContent||'').trim();if(!v)continue;const c=tc[tc.length-count+i];c.innerHTML='<strong>'+esc(v)+'</strong>';c.classList.add('olap-inline-value')}}
function boundary(rows,index,level){for(let i=index+1;i<rows.length;i++){const r=rows[i];if(r.classList.contains('olap-grand-total'))return i;if(r.classList.contains('olap-group-row')&&Number(r.dataset.olapLevel||0)<=level)return i}return rows.length}
function buildSummary(root,rows){let box=root.querySelector('.olap-ui-summary');if(!box){box=document.createElement('div');box.className='olap-ui-summary';const title=root.querySelector('.olap-modern-title');if(title)title.after(box);else root.prepend(box)}const grand=rows.find(r=>r.classList.contains('olap-grand-total'));const dataRows=rows.filter(r=>r.classList.contains('olap-data-row'));const groups=rows.filter(r=>r.classList.contains('olap-group-row'));const cashCount=groups.filter(r=>Number(r.dataset.olapLevel||0)===1).length;const total=text(grand,cells(grand).length-2)||'—';const discount=text(grand,cells(grand).length-1)||'—';box.innerHTML=`<div class="olap-summary-card olap-summary-main"><div class="olap-summary-icon">▤</div><div><span>Сумма со скидкой</span><strong>${esc(total)} <small>AZN</small></strong></div></div><div class="olap-summary-card olap-summary-discount"><div class="olap-summary-icon">%</div><div><span>Сумма скидки</span><strong>${esc(discount)} <small>AZN</small></strong></div></div><div class="olap-summary-card olap-summary-count"><div class="olap-summary-icon">●</div><div><span>Количество строк</span><strong>${dataRows.length}</strong></div></div><div class="olap-summary-card olap-summary-cash"><div class="olap-summary-icon">▣</div><div><span>Количество касс</span><strong>${cashCount||'—'}</strong></div></div>`}
function decorate(root){const table=root.querySelector('.report-table');if(!table)return;const tbody=table.querySelector('tbody');if(!tbody)return;const rows=[...tbody.children].filter(r=>r.tagName==='TR');const groups=rows.filter(r=>r.classList.contains('olap-group-row'));if(!groups.length)return;groups.forEach(group=>{const level=Number(group.dataset.olapLevel||0);group.classList.add('olap-tree-row','olap-level-'+level);const i=rows.indexOf(group),end=boundary(rows,i,level);const totals=rows.slice(i+1,end).filter(r=>r.classList.contains('olap-group-total'));if(totals.length)copyMeasures(group,totals[totals.length-1]);const toggle=group.querySelector('.olap-group-toggle');if(toggle){toggle.dataset.designReady='1';toggle.setAttribute('aria-expanded',toggle.getAttribute('aria-expanded')==='false'?'false':'true');toggle.textContent=toggle.getAttribute('aria-expanded')==='false'?'▶':'▼';toggle.title=toggle.getAttribute('aria-expanded')==='false'?'Развернуть':'Свернуть'}});rows.forEach(r=>{if(r.classList.contains('olap-data-row')){r.classList.add('olap-waiter-row');r.hidden=false}if(r.classList.contains('olap-group-total')){r.classList.add('olap-design-hidden');r.hidden=true}if(r.classList.contains('olap-grand-total')){r.classList.add('olap-tree-grand-total');r.hidden=false}});root.classList.add('olap-modern-result');let title=root.querySelector('.olap-modern-title');if(!title){title=document.createElement('div');title.className='olap-modern-title';const hs=cells(table.querySelector('thead tr')).map(c=>(c.textContent||'').trim()).filter(Boolean);title.innerHTML=`<div><div class="olap-modern-title-main">Результат отчёта</div><div class="olap-modern-subtitle">${esc(hs.slice(0,3).join('  →  ')||'Иерархический отчёт')}</div></div><div class="olap-result-actions"><span>${dataRowsCount(rows)} строк</span><button type="button" class="olap-expand-all">↘ Развернуть</button><button type="button" class="olap-collapse-all">↗ Свернуть</button></div>`;root.prepend(title);title.querySelector('.olap-expand-all').onclick=()=>setAll(root,false);title.querySelector('.olap-collapse-all').onclick=()=>setAll(root,true)}buildSummary(root,rows);bindToggles(root)}
function dataRowsCount(rows){return rows.filter(r=>r.classList.contains('olap-data-row')).length}
function setAll(root,collapse){root.querySelectorAll('.olap-group-toggle').forEach(b=>{const row=b.closest('tr.olap-group-row');if(!row)return;const level=Number(row.dataset.olapLevel||0);b.setAttribute('aria-expanded',collapse?'false':'true');b.textContent=collapse?'▶':'▼';b.title=collapse?'Развернуть':'Свернуть';row.classList.toggle('is-collapsed',collapse);let n=row.nextElementSibling;while(n){const g=n.classList.contains('olap-group-row'),l=g?Number(n.dataset.olapLevel||0):Infinity;if(g&&l<=level)break;if(!n.classList.contains('olap-design-hidden'))n.hidden=collapse;n=n.nextElementSibling}})}
function bindToggles(root){root.querySelectorAll('.olap-group-toggle').forEach(button=>{if(button.dataset.designToggleBound==='1')return;button.dataset.designToggleBound='1';button.addEventListener('click',function(e){e.preventDefault();e.stopPropagation();const row=button.closest('tr.olap-group-row');if(!row)return;const level=Number(row.dataset.olapLevel||0),collapse=button.getAttribute('aria-expanded')!=='false';let n=row.nextElementSibling;while(n){const g=n.classList.contains('olap-group-row'),l=g?Number(n.dataset.olapLevel||0):Infinity;if(g&&l<=level)break;if(!n.classList.contains('olap-design-hidden'))n.hidden=collapse;n=n.nextElementSibling}button.setAttribute('aria-expanded',collapse?'false':'true');button.textContent=collapse?'▶':'▼';button.title=collapse?'Развернуть':'Свернуть';row.classList.toggle('is-collapsed',collapse)})})}
function enhance(){const root=document.getElementById('olap-result');if(!root)return;decorate(root)}
function start(){enhance();const observer=new MutationObserver(enhance);observer.observe(document.body,{childList:true,subtree:true});let tries=0;const timer=setInterval(()=>{enhance();if(++tries>=40)clearInterval(timer)},250)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();
